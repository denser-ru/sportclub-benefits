version: '3.8'

services:
  mock-api:
    build:
      context: ./mock-api
    networks:
      - app-network

  backend:
    depends_on:
      - mock-api
    environment:
      # THIS LINE OVERRIDES THE VALUE FROM .ENV WHEN THIS FILE IS ACTIVE
      - SPORTCLUB_API_BASE_URL=http://mock-api:3004